name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t mzalcmanis/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true mzalcmanis/react-test npm test

      - run: docker build -t mzalcmanis/multi-client ./client
      - run: docker build -t mzalcmanis/multi-nginx ./nginx
      - run: docker build -t mzalcmanis/multi-server ./server
      - run: docker build -t mzalcmanis/multi-worker ./worker

      - run: docker push mzalcmanis/multi-client
      - run: docker push mzalcmanis/multi-nginx
      - run: docker push mzalcmanis/multi-server
      - run: docker push mzalcmanis/multi-worker
